import './users.scss';

const Users = () => {
    return (
        <section className="users">
            <div className="info">
                <h1>Users</h1>
                <button>Add New User</button>
            </div>
        </section>
    );
};

export default Users;
