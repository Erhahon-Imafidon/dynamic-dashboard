import './user.scss';
import { Single } from '../../components/index.tsx';

const User = () => {
    return (
        <div className="user">
            <Single />
        </div>
    );
};

export default User;
